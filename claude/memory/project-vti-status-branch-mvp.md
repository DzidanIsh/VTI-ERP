---
name: project-vti-status-branch-mvp
description: "Status kerja terkini branch VTI-Mining-Operation-Intelligence — apa yang sudah jadi, apa yang berikutnya"
metadata: 
  node_type: memory
  type: project
  originSessionId: 7256fec3-dc6c-4c0b-89b0-414dbcc1499f
  modified: 2026-08-11T23:27:43.264Z
---

Per **2026-07-27**, pekerjaan berpindah ke branch **`VTI-Mining-Operation-Intelligence`** (bukan `main`). Branch ini sengaja **hanya berisi modul Mining**; Inventory, Asset, CRM Sales, Accounting, Purchase sudah dihapus agar fokus ke 4 modul MVP blueprint. `main` masih menyimpan versi 6 modul.

**Sudah selesai & teruji end-to-end:**
- Frontend tersambung penuh ke Supabase (localStorage dibuang) — `lib/store/operasi-store.tsx` jadi satu-satunya jembatan; halaman tidak memanggil API langsung.
- Supabase Auth + 4 akun peran (`driver@` / `pengawas@` / `checker@` / `owner@vti.id`, password `VTI2026!`), middleware wajib login, guard peran BRULE-006 dua lapis (UI menyembunyikan tombol, server balas 403).
- Dashboard, Daily Production, Produktivitas, Rekap Tonase memakai angka database (bukan mock). Bagian yang masih mock diberi badge "Ilustratif".
- Asisten AI (chatbot) + laporan cetak-ke-PDF bertemplate tetap.

- **Modul per tim (2026-08-09, final commit fb7fc8d):** SATU modul "Produksi" berisi semua fitur, dengan menu & guard per peran (driver hanya Ritase Entry; pengawas/checker validasi+monitoring; owner semua). Guard halaman di `middleware.ts` via `PAGE_ROLES` dari `lib/nav.ts`; cek logika `node scripts/check-nav.ts`. Dasarnya Form D Produksi (Alfian, Mine Engineer). Modul tim lain ditambah HANYA saat form timnya masuk (lihat [[project-vti-keputusan-terkunci]]) — sempat dibuat placeholder Engineering/Plant/Survey/Manajemen lalu dihapus atas koreksi user.

- **Supabase pindah project (2026-08-09):** project lama `qqcwfwrmcbeiwvyjhgoa` terhapus (NXDOMAIN). Sempat diganti project Mumbai (`nmzncfptltchrcoqrehy`), lalu **final: `uawrargvgjlhfodproby`, region ap-southeast-1 (Singapore)**, pooler `aws-0-ap-southeast-1.pooler.supabase.com`, password DB dipakai ulang dari project lama (nilainya lihat `.env` lokal, jangan tulis di repo), API key gaya baru (`sb_publishable_`/`sb_secret_` — kompatibel dengan supabase-js/ssr). 4 migrasi + seed masuk, 4 akun auth dibuat via `node scripts/create-users.mjs`, verifikasi browser 4 peran atas modul per tim LOLOS (Mumbai diuji penuh 4 peran; Singapore diuji health + owner). Project Mumbai tinggal dihapus user dari dashboard.

- **Fungsi Form D (2026-08-09, commit 3000eb1):** dua kemampuan baru di modul Produksi, teruji end-to-end di browser — `/mining/rencana` (RencanaHarian: target harian upsert per tanggal, plan vs actual dengan angka FINAL/tervalidasi dipisah dari SEMENTARA/antrian) dan `/mining/status-unit` (KejadianOperasi: breakdown/standby/hujan/slippery per unit atau area, papan RFU, durasi, tandai selesai). Mutasi hanya Pengawas/Owner (guard server); driver tidak melihat halaman ini. Data kejadian ini kelak jadi bahan jawaban downtime untuk chatbot (tool AI belum disambungkan).

- **Pembagian peran branch (2026-08-11):** `draga` = pengembangan **mobile** (Flutter, SQL langsung, JWT sendiri); branch ini = pengembangan **website**. Konsep draga (aturan bisnis ditegakkan database) sudah dibawa ke sini — commit cbe09c7, migrasi `20260811090000_aturan_bisnis_di_database`: CHECK (status vs tahap, tonase = kapasitas×density, nilai = harga), trigger immutability (ritase final terkunci, tidak bisa dihapus, audit append-only), fungsi `setujui_ritase`/`tolak_ritase` PL/pgSQL. Route PATCH ritase kini memanggil fungsi DB; `dbError()` di `lib/api.ts` memetakan SQLSTATE→HTTP (P0002→404, 42501→403, 23000→409). Prisma & Supabase Auth TETAP dipakai — yang diadopsi cara menegakkan, bukan "tanpa ORM". Teruji: 9 asersi SQL (rollback) + browser (driver PATCH → 403 dari DB). Kelak mobile harus menunjuk DB & skema yang SAMA — skema draga (jumlah_trip rekap, catatan_tonase terpisah, tahap-0 driver) bertentangan dengan keputusan terkunci dan harus disesuaikan sebelum mobile tersambung.

- **Modul draga dipindahkan ke web (2026-08-11, commit 6aceade), atas permintaan eksplisit user — MENGGANTI keputusan lama "modul dibuat setelah form tim masuk":** modul dummy Inventory/Asset/CRM Sales/Accounting/Purchase (BannerContoh, roles owner saja) + modul HR dengan presensi berfungsi. Presensi: model Prisma `Presensi` (migrasi `20260811120000`), jam dari now() DB, unique per orang/hari, trigger anti buka-ulang; API `/api/presensi{,/masuk,/pulang,/hari-ini}`; halaman `/presensi` (semua peran) & `/hr` (hr+owner). Peran baru **"hr"** — akun `hr@vti.id` BELUM dibuat di Supabase (jalankan `node scripts/create-users.mjs`; akun lama akan bilang sudah ada, hanya hr yang baru). `PAGE_ROLES` kini union antar modul (jangan kembalikan ke timpa — /presensi ada di 2 modul). Flutter `mobile/` TIDAK dipindahkan.

- **Fitur dari Notulensi Wawancara (2026-08-11, commit 65525fc; dokumen: `C:\Users\farhan\Downloads\VTI\Notulensi_Wawancara_ERP_PT_VIP.docx`):** (a) **Service Unit** `/mining/service` — CatatanHM harian (trigger DB: HM tak boleh turun), `Unit.serviceIntervalJam` default 250 jam ILUSTRATIF (verifikasi Engineering), status OK/MENDEKATI(≤20%)/LEWAT/TANPA_DATA dihitung server, "Sudah Service" reset hmService; (b) **Laboratorium** `/lab` — peran baru "lab" (`lab@vti.id` BELUM dibuat di Supabase, jalankan `node scripts/create-users.mjs`), alur DITERIMA→PREPARASI→ANALISIS→SELESAI maju 1 tahap saja, SELESAI wajib kadar (CHECK) + terkunci (trigger) + **memperbarui `Stockpile.kadar` & `diAtasCOG` otomatis** (sumber kebenaran kadar kini lab, bukan default master); (c) **koordinat lokasi opsional di presensi** (GPS gagal ≠ gagal absen), rekap HR menautkan Google Maps. `dbError()` kini juga parse SQLSTATE dari error ORM Prisma (bukan hanya $queryRaw). **Gap notulensi yang MENUNGGU pertemuan divisi:** lembur/lost time/geofence/fingerprint (HR), penggajian-dari-ritase (Finance), format laporan lama per divisi, Logistik, notifikasi Telegram.

- **Enam arahan user (2026-08-11, commit f24112d; revisi c7bed2c):** (1) Asisten kini bernama **"Asisten AI"**, TIDAK di sidebar mana pun — pintu masuknya tombol berlogo Sparkles di topbar samping avatar (driver tidak melihat; guard `/api/chat` + `PAGE_ROLES["/mining/asisten"]` hardcode non-driver). Teks nama di topbar dihapus, identitas pindah ke kepala dropdown avatar; (2) Presensi & Profil pindah ke **dropdown avatar topbar**; `/profil` = ganti sandi (supabase.auth.updateUser) + foto profil (model `ProfilFoto` di DB, resize klien 256px, magic bytes, CHECK 512KB); (3) Rencana Produksi 3 periode — `RencanaPeriode` BULANAN/TAHUNAN, format periode CHECK regex di DB; (4) **"Rencana vs Capaian" DIHAPUS** → `/mining/dashboard-master`: kartu bulan berjalan + pembanding 2 metrik bebas (tonase/ritase/target/bbm/downtime/hadir, `/api/dashboard/serial`, `BandingLineChart` dua sumbu Y) — BBM dicatat harian per unit (`CatatanBBM`) dari halaman Service Unit; (5) `/mining/lahan` siklus SURVEY→PEMETAAN→PENAMBANGAN→REKLAMASI→SELESAI dengan riwayat `TahapanLahan` (catatan + dokumenUrl drone); (6) 52 submodul ComingSoon kini berisi **konten contoh** via `HalamanContoh` + `lib/mock-data/submodul.ts` (hapus entri saat submodul dibangun sungguhan). `PAGE_ROLES["/presensi"]` di-hardcode semua peran (hanya tercantum di nav HR).

- **Surat izin (2026-08-11, commit 04164b5):** pegawai mengajukan izin/sakit/cuti + lampiran foto surat dari halaman `/presensi` (kartu "Surat Izin"); bermuara di `/hr/cuti` (halaman SUNGGUHAN, menggantikan data contoh — route statis menang atas catch-all) tempat hr/owner setuju/tolak dengan catatan. Model `SuratIzin` (lampiran Bytes di DB, CHECK: rentang tanggal, alasan wajib, mime+data berpasangan, keputusan wajib berpenanggung jawab; trigger kunci setelah diproses). API `/api/izin{,/[id],/[id]/lampiran}`; `lib/berkas.ts` = validasi gambar bersama (dipakai foto profil juga; tipe Prisma Bytes menuntut `Uint8Array<ArrayBuffer>`, bukan Buffer). **Verifikasi browser TERTUNDA: sesi preview logout — user perlu login manual (mis. driver untuk ajukan, hr/owner untuk proses); aturan DB sudah teruji 6 asersi SQL.**

- **Perbaikan UI (2026-08-11, commit 3100d3c):** breadcrumb `/presensi` & `/profil` kini "Profil › …" (kondisi `pribadi` di topbar.tsx — jangan sampai kembali ke tagline modul); kartu Surat Izin: galat merah terpisah dari sukses hijau + teks keadaan kosong. Catatan: commit `c7bed2c` (sesi lain) memindahkan Asisten AI jadi tombol topbar (driver tidak melihat) dan menghapus teks nama di samping avatar — nama pindah ke header dropdown. **Laporan user "UI izin masih error" belum tereproduksi** (sesi preview logout, tidak bisa login sendiri) — kalau muncul lagi, minta pesan/screenshot error persisnya.

- **Home + Dashboard Master + Ollama (2026-08-11, commit 879abc3 & 0176333):** (a) navigasi ala SAP/ERPNext — `/home` launchpad kartu modul per peran, dropdown "Pilih Modul" DIHAPUS (module-switcher.tsx dihapus), `homeFor()` semua peran → `/home`, sidebar menampilkan daftar modul saat di Home; (b) Dashboard Master = model Mining Site Dashboard (KPI store nyata + cuaca + maintenance/status aset ilustratif) + kartu Banding Data; item menu "Mining Site Dashboard" dihapus (file `/mining` masih ada, tak tertaut); (c) **Asisten AI pindah ke ollama lokal** — `lib/ai/ollama.ts`: DeepSeek-R1-Distill-14B TIDAK bisa tool-calling native, jadi keluaran dipaksa `format` JSON Schema `{tipe: jawab|alat}` + loop server yang mengeksekusi alat; `think:true` (fallback tanpa think); usulan tulis disimpan sebagai metadata `usulan` di message. `.env` lokal SUDAH disetel `AI_PROVIDER="ollama"`, `OLLAMA_BASE_URL=http://192.168.110.157:11434` (alternatif: tailscale 100.89.200.100; dokumen: `C:\Users\farhan\Downloads\VTI\ollama-api-docs.html`). **BELUM teruji end-to-end: server Ollama tidak terjangkau dari mesin ini saat itu (ketiga alamat gagal) & sesi preview logout.** Model ±10,7 tok/detik — jawaban asisten akan lambat; maxDuration chat route dinaikkan ke 300.

- **Asisten AI untuk SEMUA peran termasuk driver (2026-08-12, commit 072b1cb, keputusan eksplisit user — jangan pasang lagi pengecualian driver):** tiga lapis dibuka: tombol topbar, `PAGE_ROLES["/mining/asisten"]`, guard `/api/chat`. Catatan risiko yang sudah disampaikan & diterima: alat baca asisten menjawab dari DB langsung (termasuk rekap nilai vendor) — kalau kelak perlu dibatasi, bentuknya penyaringan per-alat berdasar peran di `lib/ai/tools.ts`, bukan blokir total.

- **Tailscale + Ollama TERSAMBUNG (2026-08-12, commit d051095):** Tailscale terpasang di mesin ERP (akun `farhankun1e@`, IP 100.109.128.31); server `spark-2209` (100.89.200.100, tailnet lain) di-SHARE ke akun user — jangan logout tailscale, share putus kalau ganti akun. `.env`: `OLLAMA_BASE_URL="http://100.89.200.100:11434"`. **Uji langsung LOLOS:** think+format JSON Schema diterima server, model memilih alat dengan benar (`{"tipe":"alat","alat":"ringkasan_operasi"}`), ±20 detik/panggilan (5 detik load + ~10,7 tok/s). Gotcha yang ditemukan: model mengisi argumen opsional dengan string kosong — adapter membuangnya (`bersihkanArgumen`). Mesin ERP WiFi subnet 192.168.100.x ≠ server 192.168.110.x, jadi LAN tidak pernah jalan — Tailscale satu-satunya jalur. E2E via UI tinggal menunggu user login (driver kini juga boleh pakai asisten).

- **Sidebar terang + logo (2026-08-12, commit 72ab55b):** sidebar putih gaya ERPNext (aksen brand hijau), bisa DICIUTKAN via tombol PanelLeft topbar (state `ciut` di app-shell, diingat localStorage `sidebar-ciut`). Logo VTI (`public/logo-vti.png`, ditarik dari draga) di sidebar + login + favicon `app/icon.png`. **Gotcha middleware:** matcher kini mengecualikan `.*\\..*` (berkas ber-ekstensi) — tanpa itu gambar publik di-redirect ke login dan logo tampil rusak; jangan kembalikan matcher lama. Redirect sesudah login → `/home` (bukan `/mining`). Verifikasi visual sidebar/ciut TERTUNDA (butuh login user); halaman login terverifikasi tampil benar dengan logo.

**Berikutnya (belum dikerjakan):** sambungkan data rencana/kejadian ke tool chatbot (`lib/ai/tools.ts`) agar pertanyaan downtime terjawab; status FLAGGED + batas wajar tonase (TC-008); parameter COG/density jadi tabel konfigurasi (masih konstanta di `lib/mock-data/master.ts`); entitas master Pit; uji formal 14 test case RTM; deploy Vercel + UAT pilot. Sisa Form D untuk fase berikut: forecast, rekonsiliasi survey, PICA, notifikasi otomatis (8.3).

**Pertanyaan yang chatbot BELUM bisa jawab** karena datanya memang tidak ada: downtime per unit, dan analisis penyebab target tidak tercapai (butuh data cuaca/breakdown/BBM — modul Asset & monitoring fase 90 hari).

Lihat [[project-vti-keputusan-terkunci]], [[reference-vti-dokumen-acuan]], [[project-vti-gotcha-windows-prisma]].
